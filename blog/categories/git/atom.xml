<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">

  <title><![CDATA[Category: git | Kent's Blog]]></title>
  <link href="http://blog.kent-chiu.com/blog/categories/git/atom.xml" rel="self"/>
  <link href="http://blog.kent-chiu.com/"/>
  <updated>2014-01-01T17:03:21+08:00</updated>
  <id>http://blog.kent-chiu.com/</id>
  <author>
    <name><![CDATA[Kent]]></name>
    
  </author>
  <generator uri="http://octopress.org/">Octopress</generator>

  
  <entry>
    <title type="html"><![CDATA[在 Windwos OS 上安裝git flow]]></title>
    <link href="http://blog.kent-chiu.com/blog/2013/03/14/install-git-flow-on-windows/"/>
    <updated>2013-03-14T21:48:00+08:00</updated>
    <id>http://blog.kent-chiu.com/blog/2013/03/14/install-git-flow-on-windows</id>
    <content type="html"><![CDATA[<p>git-flow on windows主要是參考這篇文章
https://github.com/nvie/gitflow/wiki/Windows</p>

<h2>下載相關檔案</h2>

<p>git官網下載的git是屬於MSysGit版的，所以，要按照MSysGit的安裝步驟進行git flow的安裝</p>

<p>首先要先安裝<code>util-linux-ng for Windows</code>,<code>util-linux-ng for Windows</code>可以到
http://gnuwin32.sourceforge.net/packages/util-linux-ng.htm 下載，點選下圖紅框的檔案連結下次二進制文件的zip檔</p>

<p><img src="http://blog.kent-chiu.com/images/2013-03-14/2013-03-14-install-git-flow-on-windows-001.png" alt="2013-03-14-install-git-flow-on-windows-001.png" /></p>

<p>需一併下載上圖中下方紅框的<code>libintl</code>，libintl的連結會進入另一個網頁 http://gnuwin32.sourceforge.net/packages/libintl.htm，一樣下載Binaries的zip檔即可</p>

<p>再來需要確定一下git安裝的路徑，如果是32位元的os會是在 <code>C:\Program Files\Git</code>, 64位元的os會是在 <code>C:\Program Files (x86)\Git</code> (<strong>以下的步驟以64位元的os的git安裝目錄為例</strong>)</p>

<p>下載後的 util-linux-ng-2.14.1-bin.zip 解壓，可以在<code>util-linux-ng-2.14.1-bin\bin</code>找到 getopt.exe
下載後的libintl-0.14.4-bin.zip解壓，可以在<code>libintl-0.14.4-bin\bin</code>找到 libintl3.dll</p>

<p>將getopt.exe跟 libintl3.dll copy到git安裝目錄 <code>C:\Program Files (x86)\Git</code></p>

<h2>clone git flow</h2>

<p>clone github上的gitflow到臨時性的工作目錄 <code>c:\gitflow</code> (<em>這個目錄做完安裝後可以刪除</em>)</p>

<p>```</p>

<pre><code>git clone --recursive git://github.com/nvie/gitflow.git c:\gitflow
</code></pre>

<p>```</p>

<p>clone完成後，到 c:\gitflow\contrib 執行</p>

<p>```</p>

<pre><code>C:\gitflow\contrib&gt;msysgit-install.cmd "C:\Program Files (x86)\Git"
</code></pre>

<p>```</p>

<p>執行後，會看到一些檔案被複制到git安裝目錄</p>

<p>```</p>

<pre><code>C:\gitflow\git-flow -&gt; C:\Program Files (x86)\Git\bin\git-flow
C:\gitflow\git-flow-feature -&gt; C:\Program Files (x86)\Git\bin\git-flow-feature
C:\gitflow\git-flow-hotfix -&gt; C:\Program Files (x86)\Git\bin\git-flow-hotfix
C:\gitflow\git-flow-init -&gt; C:\Program Files (x86)\Git\bin\git-flow-init
C:\gitflow\git-flow-release -&gt; C:\Program Files (x86)\Git\bin\git-flow-release
C:\gitflow\git-flow-support -&gt; C:\Program Files (x86)\Git\bin\git-flow-support
C:\gitflow\git-flow-version -&gt; C:\Program Files (x86)\Git\bin\git-flow-version
已複製 7 個檔案
C:\gitflow\gitflow-common -&gt; C:\Program Files (x86)\Git\bin\gitflow-common
C:\gitflow\gitflow-shFlags -&gt; C:\Program Files (x86)\Git\bin\gitflow-shFlags
已複製 2 個檔案
C:\gitflow\shFlags\src\shflags -&gt; C:\Program Files (x86)\Git\bin\gitflow-shFlags
已複製 1 個檔案
</code></pre>

<p>```</p>

<p>這樣整個安裝步驟就全部完成了</p>

<h2>試車</h2>

<p>可以在windows的command  windows裡打git flow，如果有看到類似以下的訊息就ok了</p>

<p>```</p>

<pre><code>C:\gitflow\contrib&gt;git flow
usage: git flow &lt;subcommand&gt;

Available subcommands are:
   init      Initialize a new git repo with suppo
   feature   Manage your feature branches.
   release   Manage your release branches.
   hotfix    Manage your hotfix branches.
   support   Manage your support branches.
   version   Shows version information.

Try 'git flow &lt;subcommand&gt; help' for details.
</code></pre>

<p>```</p>

<p>到這裡都ok的話，之前的臨時性的工作目錄 <code>c:\gitflow</code>可以刪了它</p>
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[在Windows上安裝Git Server]]></title>
    <link href="http://blog.kent-chiu.com/blog/2012/01/05/install_git_server_on_windows/"/>
    <updated>2012-01-05T00:00:00+08:00</updated>
    <id>http://blog.kent-chiu.com/blog/2012/01/05/install_git_server_on_windows</id>
    <content type="html"><![CDATA[<p>在Windows安裝git，讓每個經密碼認証後的user可以透過http pull跟push資料.</p>

<p>安裝環境如下:</p>

<ol>
<li>OS : WinXP</li>
<li>Web Server: Apache (xampp-win32-1.7.7-VC9)</li>
<li>Git Client: msysgit(Git-1.7.8-preview20111229-unicode.exe)</li>
</ol>


<p>xampp解壓到c:\xampp</p>

<p>git安裝到C:\Program Files\Git</p>

<p><code>
copy C:\Program Files\Git\libiconv-2.dll C:\Program Files\Git\libexec\git-core
copy C:\Program Files\Git\libiconv2.dll C:\Program Files\Git\libexec\git-core
</code></p>

<p><code>
git init --bare TestProject
</code></p>

<p>這下面這段加到最後面</p>

<p>```
Alias /git "c:/GitRepos"
<Directory "c:/GitRepos"></p>

<pre><code>Options Indexes FollowSymLinks MultiViews
AllowOverride None
Order Allow,Deny
Allow from all
</code></pre>

<p></Directory>
 </p>

<h1>Set this to the root folder containing your Git repositories.</h1>

<p>SetEnv GIT_PROJECT_ROOT C:/GitRepos
 </p>

<h1>Set this to export all projects by default (by default,</h1>

<h1>git will only publish those repositories that contain a</h1>

<h1>file named “git-daemon-export-ok”</h1>

<p>SetEnv GIT_HTTP_EXPORT_ALL
 </p>

<h1>Route specific URLS matching this regular expression to the git http server.</h1>

<p>ScriptAliasMatch \
  "(?x)^/git/(.*/(HEAD | \</p>

<pre><code>info/refs | \
objects/(info/[^/]+ | \
  [0-9a-f]{2}/[0-9a-f]{38} | \
  pack/pack-[0-9a-f]{40}\.(pack|idx)) | \
git-(upload|receive)-pack))$" \
"C:/Program Files/git/libexec/git-core/git-http-backend.exe/$1"
</code></pre>

<p>```</p>

<p>restart apache</p>

<p>```</p>

<h1>LoadModule dav_module modules/mod_dav.so</h1>

<h1>LoadModule dav_fs_module modules/mod_dav_fs.so</h1>

<p> </p>

<h1>Distributed authoring and versioning (WebDAV)</h1>

<h1>Attention! WEB_DAV is a security risk without a new userspecific configuration for a secure authentifcation</h1>

<p>Include "conf/extra/httpd-dav.conf"
```</p>
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[Setup Git Server]]></title>
    <link href="http://blog.kent-chiu.com/blog/2011/10/17/setup_git_server/"/>
    <updated>2011-10-17T00:00:00+08:00</updated>
    <id>http://blog.kent-chiu.com/blog/2011/10/17/setup_git_server</id>
    <content type="html"><![CDATA[<p>In this article, we will setup a git server from team access. It is
mimic the <a href="http://whygitisbetterthanx.com/#any-workflow" title="http://whygitisbetterthanx.com/#any-workflow">SVN style
workflow</a>(Centralized
Workflow), it's a easy way for integrating all code in one place.</p>

<h1>Main Steps</h1>

<ol>
<li><a href="#init_bare_repository" title="git:setup_git_server ↵">init bare
repository</a></li>
<li><a href="#add_user" title="git:setup_git_server ↵">add user</a></li>
<li><a href="#first_commited" title="git:setup_git_server ↵">first commited</a></li>
<li><a href="#clone" title="git:setup_git_server ↵">clone</a></li>
</ol>


<h4>init bare repository</h4>

<p>create a bare repository in /opt/git/project.git</p>

<p><code>
$ git --bare init
Initialized empty Git repository in /opt/git/project.git/
</code></p>

<h4>add user</h4>

<p>add a user called 'git' for executing git commonds.</p>

<p><code>
$ adduser git
</code></p>

<p>change git's password</p>

<p><code>
$ passwd git
</code></p>

<h4>first commited</h4>

<p>```</p>

<h1>on SomeBoday's computer</h1>

<p>$ cd myproject
$ git init
$ git add .
$ git commit -m 'initial commit'
$ git remote add origin git@gitserver:/opt/git/project.git
$ git push origin master
```</p>

<h4>clone</h4>

<p><code>
git clone git@gitserver:/opt/git/project.git
</code></p>
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[GIT 101]]></title>
    <link href="http://blog.kent-chiu.com/blog/2011/10/17/git_101/"/>
    <updated>2011-10-17T00:00:00+08:00</updated>
    <id>http://blog.kent-chiu.com/blog/2011/10/17/git_101</id>
    <content type="html"><![CDATA[<p>NOTICE: The most data in this page is come from <a href="http://progit.org/book/" title="http://progit.org/book/">Pro
Git</a>.</p>

<h2>Basic</h2>

<h3>The Three States</h3>

<ol>
<li>committed - the data is safely stored in your local database</li>
<li>modified - file is changed but have not committed</li>
<li>staged - marked a modified file in its current version to go into
your next commit snapshot.</li>
</ol>


<p><img src="http://blog.kent-chiu.com/images/2011-10-17/git_101_001.png" alt="git_101_001.png" /></p>

<ul>
<li>The Git directory is where Git stores the metadata and object
database for your project</li>
<li>The working directory is a single checkout of one version of the
project.</li>
<li>The staging area is a simple file, generally contained in your Git
directory, that stores information about what will go into your next
commit. It’s sometimes referred to as the index, but it’s becoming
standard to refer to it as the staging area.</li>
</ul>


<h4>Basic Git workflow</h4>

<ol>
<li>You modify files in your working directory.</li>
<li>You stage the files, adding snapshots of them to your staging area.</li>
<li>You do a commit, which takes the files as they are in the staging
area and stores that snapshot permanently to your Git directory.</li>
</ol>


<h4>Install Git</h4>

<h5>Windows</h5>

<p>download <a href="http://code.google.com/p/msysgit">msysgit</a> and install it.</p>

<h5>Linux</h5>

<pre><code>$ yum install git-core


$ apt-get install git-core
</code></pre>

<h4>Terms</h4>

<ul>
<li>HEAD : 一個特殊的指標(pointer)，它指向current branch.</li>
<li>master : 預設的branch</li>
<li>origin : 預設遠端的master branch</li>
<li>remote : 遠端 repository</li>
<li>tracking branch: 從遠端fetch到local端的branch</li>
<li>rebase :
將原先branch出來的時間點(版本)向前或向後移動，這樣可以讓該branch移除或加入功能別的branch的功能</li>
<li><p>cherry pick commit : 只挑選同一branch的部份commits做merge</p>

<pre><code>A - B - C - D - E   master
  \ - X - Y  - Z   branch 1
</code></pre></li>
</ul>


<h5>rebase</h5>

<p>本來branch 1不包含B特性，可以透過rebase將B包含進去</p>

<pre><code>A - B - C - D - E   master
      \ - X - Y  - Z   branch 1
</code></pre>

<h5>cherry pick commit</h5>

<p>如果將branch 1 merge 回master，會包含X,Y,Z特性，但可以透過cherry pick
commit，則可以選擇merge時不包含X,或X,Y</p>

<pre><code>A - B - C - D - E    master
  \ - X - Y  - Z   branch 1
</code></pre>

<h4>Tracking Branches</h4>

<p>跟遠端repository的branch有對應關係的local branch，叫tracking branch，如果branch是一個tracking branch
那麼push時，會自動push到遠端的對應的branch，如果不是tracking branch，就需要特別指定要push到遠端的那一個branch</p>

<p>像這樣 <code>git push origin master</code>，我們在init git repository後，第一次的push需要下這個commit，就是
因為一開始local的(master) barnch 沒有跟遠端的關聯，所以我們要跟git說要push到遠端(預設遠端的名字叫origin,當然，
你也可以取其他的名稱ex: github，bitbucket或mycompany)的master上</p>

<h2>Command</h2>

<p>從遠端fetch到local端的branch是無法直接更改的，想要更改必須在建立自已新的branch</p>

<pre><code>checkout -b branch1  remote_server/branch1
checkout -b feature/peak --track origin/feature/peak
</code></pre>

<h4>config</h4>

<p>config有三個等級，由小到大分別為</p>

<ol>
<li>庫等級(repository裡)，只會單一的repository有影響</li>
<li>全局(global)，放在user home，對user會存取的庫有影響</li>
<li>系統等級(system),放在系統的配置目錄下(ex: /etc),對所有的user有影響</li>
</ol>


<p>而優先權則相反，以repository的為主，再來是global的，最後才是system的，很直覺也很合理</p>

<h5>list config</h5>

<pre><code>$ git config --list
user.name=Scott Chacon
user.email=schacon@gmail.com
color.status=auto
color.branch=auto
color.interactive=auto
color.diff=auto
...
</code></pre>

<p>config是git跟svn最大不同的地方，git必需要正確定user info後，操作會才正常</p>

<h5>config user name and email</h5>

<pre><code>$ git config --global user.name "Kent Chiu"
$ git config --global user.email kent.chiu@example.com
</code></pre>

<h5>clone from windows local</h5>

<pre><code>file:///d:/PATH/PROJECT_HOME
</code></pre>

<h4>add</h4>

<p>add是個多功能的指令，它可以</p>

<ul>
<li>把untrack file變成tracked file</li>
<li>把modified file變成staged file</li>
</ul>


<p>或commit時同時做add的動作(不建議)</p>

<pre><code>$ git add -a -m 'comment'
</code></pre>

<h4>commit</h4>

<pre><code>$ git add *.c
$ git add README
# files need be staged before commit
$ git commit -m 'initial project version'
</code></pre>

<p>提交所有控版下的檔案</p>

<pre><code>$ git commit -a -m 'comment'
# 也可以把 a (all) m (message)合在一起用 
$ git commit -am 'comment'
</code></pre>

<p>如果對前一次提交的訊息不滿意，可以透過<code>amend</code>做修改</p>

<pre><code>$ git commit --amend -m 'modify comment'
</code></pre>

<h4>rm</h4>

<p>remove files. 如果是直接從file system刪檔，而沒透過git指令，那該檔案的狀態會變成 <strong>Changed but not updated</strong> (unstaged)</p>

<blockquote><p>上面的<code>add</code> command並不會把檔案刪除的動作加到stage裡</p></blockquote>

<h5>remove file and delete it</h5>

<pre><code>$git rm myfile.txt
</code></pre>

<h5>remove files from staged</h5>

<p>untracking files but keep in file system.
這個指定通常用在想把版控的檔案變成非版控</p>

<pre><code>$git rm --staged myfile.txt
</code></pre>

<h5>清除untracked files</h5>

<p>如果想快速的清除還沒有加到 index 的 file ，可以用以下的指令</p>

<pre><code>$git clean -f -d
</code></pre>

<p>參數 <code>-d</code> 是指連 untracked 的目錄一併清除</p>

<h4>mv</h4>

<p>搬移/更名</p>

<pre><code>$ git  README README.txt
</code></pre>

<h4>status</h4>

<p>顯示異動檔案狀態</p>

<h4>log</h4>

<p>顯示異動檔案細節</p>

<pre><code>$ git log
</code></pre>

<h5>常用參數</h5>

<ul>
<li>-p 列出細節</li>
<li>-n n表數字，列出最新的幾個</li>
<li>–graph 祖譜圖</li>
<li>–pretty 改變預設的輸出格式</li>
<li>–pretty:format自定輸出格式 <code>$ git log --pretty=format:"%h - %an, %ar : %s"</code></li>
</ul>


<h5>過濾(limit by commit attributes)</h5>

<pre><code>$ git log --author=kent
$ git log --committer=kent 
$ git log --grep="commit .*message"
</code></pre>

<h5>search for a change</h5>

<pre><code>$ git log -S"Some code change"
$ git log --pickaxe-regex -S "some.*text"
</code></pre>

<h5>limit by changes to specific path</h5>

<pre><code>git lot -- some/file
</code></pre>

<h4>遠端相關的指令</h4>

<h5>show remote repositories</h5>

<pre><code>$ git remote -v
origin  git://github.com/schacon/ticgit.git
</code></pre>

<h5>adding remote repositories</h5>

<pre><code>$ git remote add pb git://github.com/paulboone/ticgit.git
$ git remote -v
origin  git://github.com/schacon/ticgit.git
pb  git://github.com/paulboone/ticgit.git
</code></pre>

<h4>gitk</h4>

<p>Using a GUI to Visualize History.</p>

<h4>mergetool</h4>

<p>Calling merge tool.</p>

<h4>reset</h4>

<p>將檔案unsatged</p>

<p>```
  工作區         暫存區          HEAD</p>

<pre><code>| &lt;---------- |                       git reset         重置加入暫存區的動作(也就是取消 git add) 
              | &lt;---------- |         git reset --soft  重置加入版本庫的動作(也就是取消commited)，但commmit過的檔案會保留在工作區                
| &lt;---------- | &lt;---------- |         git reset --hard  完整的取消整個commit，工作區會的檔案會被恢復到送個版本
</code></pre>

<p>```</p>

<blockquote><p><code>git reset</code>, <code>git reset HEAD</code>, <code>git reset --mixed</code>這三個指令做的是一樣的事
<code>git reset --hard</code> 使用上要小心，commit的歷史記錄會不見</p></blockquote>

<p>使用時機</p>

<ul>
<li><code>git reset</code>          不小心加入不想加入暫存取(index)的檔案，可用這個取消</li>
<li><code>git reset --soft</code>   想重新修正某一次的commit (<code>git commit --amend</code>就是透過這個指令完成的)</li>
<li><code>git reset --hard</code>   回到某個版本，就像完做過異動一樣，merge失敗時，也常用這個來恢復到merged前的狀態</li>
</ul>


<blockquote><p><code>git reset --hard</code> 只會退回到上一個commit，如果要退更多，就要直接指定commit id <code>git reset --hard &lt;commit id&gt;</code></p></blockquote>

<h4>diff</h4>

<p>```
  工作區         暫存區          HEAD</p>

<pre><code>| &lt;---------&gt; |                       git diff                  比對工作區跟暫存區的差異
              | &lt;---------&gt; |         git diff --staged         比對HEAD跟暫存區的差異
| &lt;-----------------------&gt; |         git diff HEAD             比對HEAD跟工作區的差異
                        ┌-&gt; |
                        |   |         git diff $commit $commit  比對兩個commits間的差異
                        └-&gt; |
</code></pre>

<p>```</p>

<blockquote><p>如果是要比對某個檔在在不同的 commits 間的差異，可使用以下指令
<code>git diff $commit $commit -- path/file</code></p></blockquote>

<h4>解讀diff</h4>

<pre><code>@@ -3,7 +3,7 @@
 &lt;h1&gt;Tag Colud&lt;/h1&gt;
 &lt;ul&gt;
   &lt;li&gt;&lt;a href='http://blog.kent-chiu.com/blog/categories/actionbar'&gt;Actionbar (1)&lt;/a&gt;&lt;/li&gt;
-  &lt;li&gt;&lt;a href='http://blog.kent-chiu.com/blog/categories/android'&gt;Android (26)&lt;/a&gt;&lt;/li&gt;
+  &lt;li&gt;&lt;a href='http://blog.kent-chiu.com/blog/categories/android'&gt;Android (24)&lt;/a&gt;&lt;/li&gt;
   &lt;li&gt;&lt;a href='http://blog.kent-chiu.com/blog/categories/apache'&gt;Apache (8)&lt;/a&gt;&lt;/li&gt;
   &lt;li&gt;&lt;a href='http://blog.kent-chiu.com/blog/categories/broadcast-receiver'&gt;Broadcast_receiver (2)&lt;/a&gt;&lt;
   &lt;li&gt;&lt;a href='http://blog.kent-chiu.com/blog/categories/cdi'&gt;Cdi (2)&lt;/a&gt;&lt;/li&gt;
</code></pre>

<p>diff中的資訊包含兩部份，第一部份是異動概要 <code>@@ -3,7 +3,7 @@</code>，第二部份是異動細節
第一部份的是資訊 <code>@@ -3,7 +3,7 @@</code>是說原始文件從第3行開始，後面的7行(也就是第3~10行)異動成為新文件的第3行到第7行(也是第3~10行)，本例中因為是調整了某一行的內容，所以異動範圍都是七行，如果新的文件的範圍可能更大(ex:加了幾行)，或更小(刪了幾行)。</p>

<blockquote><p>上述的異動範為7行，是指相關的上下文共有七行，而不是這七行都有改到</p></blockquote>

<p>第二部份比較直覺，就上例中的第六行</p>

<pre><code>-  &lt;li&gt;&lt;a href='http://blog.kent-chiu.com/blog/categories/android'&gt;Android (26)&lt;/a&gt;&lt;/li&gt;
</code></pre>

<p>及第七行</p>

<pre><code>+  &lt;li&gt;&lt;a href='http://blog.kent-chiu.com/blog/categories/android'&gt;Android (24)&lt;/a&gt;&lt;/li&gt;
</code></pre>

<p>代表著第六行的內容被異動成第七行</p>

<h4>show</h4>

<h5>review last commit</h5>

<pre><code>$ git show
 
### output ###
 
commit b8d5b1e2944d1785a3e8ca15979f9c462eb611c9
Author: Kent Chiu &lt;kent.chiu@techmore.com.tw&gt;
Date:   Thu Feb 18 13:07:49 2010 +0800
 
    foo
 
diff --git a/dir/foo b/dir/foo
new file mode 100644
index 0000000..257cc56
--- /dev/null
+++ b/dir/foo
@@ -0,0 +1 @@
+foo
</code></pre>

<h5>show the stats</h5>

<pre><code>$ git show --stat
 
### output ###
 
commit b8d5b1e2944d1785a3e8ca15979f9c462eb611c9
Author: Kent Chiu &lt;kent.chiu@techmore.com.tw&gt;
Date:   Thu Feb 18 13:07:49 2010 +0800
 
    foo
 
 dir/foo |    1 +
 1 files changed, 1 insertions(+), 0 deletions(-)
</code></pre>

<h5>show SVN style status</h5>

<pre><code>$ git show --name-status
 
### output ###
 
commit b8d5b1e2944d1785a3e8ca15979f9c462eb611c9
Author: Kent Chiu &lt;kent.chiu@techmore.com.tw&gt;
Date:   Thu Feb 18 13:07:49 2010 +0800
 
    foo
 
A       dir/foo
</code></pre>

<h4>push</h4>

<h5>deleting remote branches</h5>

<pre><code>$ git push origin :serverfix
To git@github.com:schacon/simplegit.git
 - [deleted]         serverfix
</code></pre>

<p>This is called by</p>

<pre><code>$ git push [remotename] [localbranch]:[remotebranch]
$ git push origin                    :serverfix 
</code></pre>

<p>let localbranch blank means deleted it.</p>

<blockquote><p>git 1.7之後提供這個指令可以刪遠端server <code>git push origin --delete &lt;branchName&gt;</code></p></blockquote>

<h4>git 全域設定檔</h4>

<p>ini git configuration</p>

<p>```
[user]</p>

<pre><code>name = kent
email = kent@mail.com
</code></pre>

<p>[alias]</p>

<pre><code>st = status
ci = commit
co = checkout
br = branch
</code></pre>

<p>[color]</p>

<pre><code>ui = true
diff = auto
status = auto
branch = auto
</code></pre>

<p>```</p>

<h2>常用指令</h2>

<h4>檢視 fetch 下來但尚未被 merged 的資料</h4>

<p>從遠端 fetch 新資料下來，如果想先在做 merge 前先查看資料的異動狀況，可以透過 log 跟 diff，log 可以查看 commits 的記錄，而 diff 可以對檔案內容做比對</p>

<p>查看 fetch 下來，等著被 merged 的 commits</p>

<p>```</p>

<pre><code>$ git fetch                 # 或者是用 git remote update 來 fetch 所有 branches
$ git log HEAD..origin      # 在pull (也就是 merge ) 前，可以先用這個指令查看那些 commits 會被 merged
$ git diff HEAD...origin    # 在pull (也就是 merge ) 前，可以先用這個指令查看那些 diffs 會被 merged
</code></pre>

<p>```</p>

<blockquote><p>執行 fetch 是把 server 上的 branch 更新到 local repository 的 remote branch
所以需要進行差異性檢視的內容是 HEAD 到 origin</p></blockquote>

<h4>checkout 新的 remote branch 並 tracking</h4>

<p>之前常會發生 checkout 每個 branch後，要 push 遠端server時，卻都只是 push 到 local，這是因為預設的 checkout 指令是不會去 tracking remote branch
所以，如果要 checkout 後能夠讓 branch 成為 track branch，就要加上 <code>--track</code> 的參數</p>

<p>```</p>

<pre><code>$ checkout -b new_branch --track origin/branch_name 
</code></pre>

<p>```</p>

<h1>Resource</h1>

<ul>
<li>幾種merge的手法 <a href="http://eclipsesource.com/blogs/2011/05/30/merging-branches-in-eclipse-git-egit/">http://eclipsesource.com/blogs/2011/05/30/merging-branches-in-eclipse-git-egit/</a></li>
</ul>

]]></content>
  </entry>
  
</feed>
